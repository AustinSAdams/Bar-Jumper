/* This page is for the "/bars" extension on the website. It should have:
    1. Default Theme Colors.
    2. Easy to navigate menu.
    3. Buttons containing links to original page, with currect bar select.

    To access this page locally, after running "npm run dev", go into your web browser
    and type in "http://localhost:3000/bars".
*/

export default function Page(){
    return (
    <div>
        <p>Bars Page</p>
    </div>
    );
}